# Multi-phase plan

Use when the user wants an execution plan for dependent phases or changes.

Use [plan](../../plan/SKILL.md) to define outcomes, dependencies, ownership, and proof. Include delivery or stack topology only when the requested workflow needs it.

Planning alone ends with the plan and open decisions. If the user has already requested implementation too, continue with the settled units rather than treating this planning step as a new approval gate.
