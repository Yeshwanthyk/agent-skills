interface:
  display_name: "Orchestrate"
  short_description: "Route substantial work through verified agent lanes"
  default_prompt: "Use $orchestrate to route Luna-high scouts, Luna-max thin slices, Sol-medium thick slices, and Sol-medium verification without model fallback."
policy:
  allow_implicit_invocation: false
