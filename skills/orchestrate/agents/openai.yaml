interface:
  display_name: "Orchestrate"
  short_description: "Route substantial work through verified agent lanes"
  default_prompt: "Use $orchestrate to run Luna scouts, implementation, and lint lanes with independent Sol review."
policy:
  allow_implicit_invocation: false
