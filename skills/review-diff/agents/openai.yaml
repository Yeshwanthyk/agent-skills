interface:
  display_name: "Review Diff"
  short_description: "Annotate a worktree or PR/MR diff"
  default_prompt: "Use $review-diff to open the current worktree or supplied PR/MR in the bundled local annotation workspace, then let me copy the feedback back into chat."
policy:
  allow_implicit_invocation: true
