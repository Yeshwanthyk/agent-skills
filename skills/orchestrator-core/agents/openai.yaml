interface:
  display_name: "Orchestrator Core"
  short_description: "Coordinate workers, titles, and central task state."
  default_prompt: "Use $orchestrator-core to coordinate worker threads, keep titles accurate, and maintain a central task ledger."
